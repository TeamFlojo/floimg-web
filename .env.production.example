# floimg-web Production Environment
# Static site - minimal runtime configuration needed

# Build-time: Set in Astro config or at build time
# PUBLIC_API_URL=https://api.floimg.com
# PUBLIC_STUDIO_URL=https://studio.floimg.com

# Note: This is a static site served by nginx.
# Environment variables are baked in at build time.
# For Coolify, set these as build arguments if needed.
