---
import MarketingLayout from '../layouts/MarketingLayout.astro';

const examples = [
  {
    title: 'Sales Dashboard',
    description: 'Bar chart with quarterly revenue data',
    generator: 'quickchart',
    category: 'Charts',
  },
  {
    title: 'User Growth',
    description: 'Line chart showing monthly active users',
    generator: 'quickchart',
    category: 'Charts',
  },
  {
    title: 'Market Share',
    description: 'Pie chart with competitor analysis',
    generator: 'quickchart',
    category: 'Charts',
  },
  {
    title: 'API Flow',
    description: 'Sequence diagram for REST endpoints',
    generator: 'mermaid',
    category: 'Diagrams',
  },
  {
    title: 'System Architecture',
    description: 'Microservices topology diagram',
    generator: 'mermaid',
    category: 'Diagrams',
  },
  {
    title: 'State Machine',
    description: 'Order processing state diagram',
    generator: 'mermaid',
    category: 'Diagrams',
  },
  {
    title: 'Event Registration',
    description: 'QR code linking to signup page',
    generator: 'qr',
    category: 'QR Codes',
  },
  {
    title: 'WiFi Access',
    description: 'QR code for network credentials',
    generator: 'qr',
    category: 'QR Codes',
  },
  {
    title: 'Force Graph',
    description: 'Network visualization with D3',
    generator: 'd3',
    category: 'Visualizations',
  },
  {
    title: 'Treemap',
    description: 'Hierarchical data visualization',
    generator: 'd3',
    category: 'Visualizations',
  },
  {
    title: 'Landing Page',
    description: 'Full-page website screenshot',
    generator: 'screenshot',
    category: 'Screenshots',
  },
  {
    title: 'Social Card',
    description: 'Open Graph image from HTML',
    generator: 'screenshot',
    category: 'Screenshots',
  },
];

const categories = [...new Set(examples.map(e => e.category))];
---

<MarketingLayout title="Gallery" description="Example images generated with floimg">
  <main>
    <!-- Hero -->
    <section class="bg-gradient-to-b from-violet-50 to-white dark:from-violet-950/50 dark:to-zinc-950 py-16">
      <div class="max-w-6xl mx-auto px-6">
        <h1 class="text-4xl font-bold text-center mb-4 text-zinc-900 dark:text-white">Gallery</h1>
        <p class="text-xl text-zinc-600 dark:text-zinc-400 text-center max-w-2xl mx-auto">
          Explore examples of images generated with floimg. Each example shows what's possible
          with our generators.
        </p>
      </div>
    </section>

    <!-- Filter tabs -->
    <section class="border-b border-zinc-200 dark:border-white/10 sticky top-0 bg-white dark:bg-zinc-950 z-10">
      <div class="max-w-6xl mx-auto px-6">
        <div class="flex gap-1 overflow-x-auto py-4" id="category-tabs">
          <button
            class="category-tab px-4 py-2 rounded-full text-sm font-medium bg-violet-600 text-white"
            data-category="all"
          >
            All
          </button>
          {categories.map(category => (
            <button
              class="category-tab px-4 py-2 rounded-full text-sm font-medium bg-zinc-100 dark:bg-zinc-800 text-zinc-600 dark:text-zinc-400 hover:bg-zinc-200 dark:hover:bg-zinc-700"
              data-category={category}
            >
              {category}
            </button>
          ))}
        </div>
      </div>
    </section>

    <!-- Gallery grid -->
    <section class="py-16 bg-white dark:bg-zinc-950">
      <div class="max-w-6xl mx-auto px-6">
        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6" id="gallery-grid">
          {examples.map(example => (
            <article
              class="gallery-item bg-white dark:bg-zinc-900 rounded-xl border border-zinc-200 dark:border-white/10 overflow-hidden hover:shadow-lg dark:hover:shadow-violet-500/10 transition-shadow"
              data-category={example.category}
            >
              <!-- Placeholder for generated image -->
              <div class="aspect-video bg-gradient-to-br from-zinc-100 to-zinc-200 dark:from-zinc-800 dark:to-zinc-900 flex items-center justify-center">
                <div class="text-center p-4">
                  <div class="w-12 h-12 mx-auto mb-2 rounded-lg bg-violet-100 dark:bg-violet-900/50 flex items-center justify-center">
                    <span class="text-violet-600 dark:text-violet-400 text-xl">
                      {example.generator === 'quickchart' && '📊'}
                      {example.generator === 'mermaid' && '📐'}
                      {example.generator === 'qr' && '📱'}
                      {example.generator === 'd3' && '📈'}
                      {example.generator === 'screenshot' && '🖼️'}
                    </span>
                  </div>
                  <span class="text-xs text-zinc-500 dark:text-zinc-400">Generated with {example.generator}</span>
                </div>
              </div>
              <div class="p-4">
                <div class="flex items-start justify-between gap-2 mb-2">
                  <h3 class="font-semibold text-zinc-900 dark:text-white">{example.title}</h3>
                  <span class="text-xs px-2 py-1 rounded-full bg-zinc-100 dark:bg-zinc-800 text-zinc-600 dark:text-zinc-400 whitespace-nowrap">
                    {example.category}
                  </span>
                </div>
                <p class="text-sm text-zinc-600 dark:text-zinc-400 mb-3">{example.description}</p>
                <button class="text-sm text-violet-600 dark:text-violet-400 hover:text-violet-700 dark:hover:text-violet-300 font-medium">
                  View code →
                </button>
              </div>
            </article>
          ))}
        </div>
      </div>
    </section>

    <!-- CTA -->
    <section class="py-16 bg-zinc-50 dark:bg-zinc-900/50">
      <div class="max-w-4xl mx-auto px-6 text-center">
        <h2 class="text-2xl font-bold mb-4 text-zinc-900 dark:text-white">Build Your Own</h2>
        <p class="text-zinc-600 dark:text-zinc-400 mb-8">
          Ready to create images like these? Get started with floimg in minutes.
        </p>
        <div class="flex flex-wrap justify-center gap-4">
          <a
            href="/docs/getting-started/quick-start"
            class="px-6 py-3 bg-violet-600 text-white rounded-lg font-medium hover:bg-violet-700"
          >
            Quick Start Guide
          </a>
          <a
            href="https://studio.floimg.com"
            class="px-6 py-3 bg-white dark:bg-zinc-800 border border-zinc-200 dark:border-white/10 text-zinc-700 dark:text-zinc-300 rounded-lg font-medium hover:bg-zinc-50 dark:hover:bg-zinc-700"
          >
            Try floimg Studio
          </a>
        </div>
      </div>
    </section>
  </main>

  <script>
    // Category filtering
    const tabs = document.querySelectorAll('.category-tab');
    const items = document.querySelectorAll('.gallery-item');

    tabs.forEach(tab => {
      tab.addEventListener('click', () => {
        const category = tab.dataset.category;

        // Update active tab
        tabs.forEach(t => {
          t.classList.remove('bg-violet-600', 'text-white');
          t.classList.add('bg-zinc-100', 'dark:bg-zinc-800', 'text-zinc-600', 'dark:text-zinc-400');
        });
        tab.classList.remove('bg-zinc-100', 'dark:bg-zinc-800', 'text-zinc-600', 'dark:text-zinc-400');
        tab.classList.add('bg-violet-600', 'text-white');

        // Filter items
        items.forEach(item => {
          if (category === 'all' || item.dataset.category === category) {
            item.style.display = 'block';
          } else {
            item.style.display = 'none';
          }
        });
      });
    });
  </script>
</MarketingLayout>
